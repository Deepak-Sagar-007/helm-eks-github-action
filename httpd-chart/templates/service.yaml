apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-httpd-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: httpd
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
